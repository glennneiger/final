module.exports = (app, con, bodyParser) => {
// var user
// var userAdd = "SELECT username, followersCount, picture FROM users WHERE username LIKE "
// var sqlSelect
app.post('/postMaker', (request, response) => {

  // con.query(sqlSelect, function (error, result) {
  //   console.log(result)
  //   response.send(result)
  //   if (error) throw error
  //   })
})
}
