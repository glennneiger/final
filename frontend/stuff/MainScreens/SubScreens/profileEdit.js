import React, { Component } from "react";
import {
  Text,
  View,
  TextInput,
  TouchableOpacity,
  AsyncStorage
} from "react-native";
import styles from "../../styles";

export default class ProfileEdit extends Component {
  render() {
    return (<View style={[styles.whiteBackground, styles.fullScreen]}>
        <View style={styles.commentListItem}>
        </View>
    </View>)
  }
}
